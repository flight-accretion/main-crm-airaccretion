@extends('admin.layouts.header')
@section('content')
@php($editing = $item->exists)
<div class="block justify-between page-header md:flex"><h3 class="text-xl font-semibold">{{ $editing ? 'Edit' : 'Add' }} IVR Agent Mapping</h3></div>
<div class="box"><div class="box-body"><form method="POST" action="{{ $editing ? route('admin.ivr.agents.update',$item) : route('admin.ivr.agents.store') }}">@csrf @if($editing) @method('PUT') @endif
<div class="grid grid-cols-12 gap-4"><div class="col-span-12 md:col-span-6"><label class="ti-form-label">VI Agent Number *</label><input name="vi_agent_number" class="form-control" value="{{ old('vi_agent_number',$item->vi_agent_number) }}" required></div><div class="col-span-12 md:col-span-6"><label class="ti-form-label">VI Agent Name</label><input name="vi_agent_name" class="form-control" value="{{ old('vi_agent_name',$item->vi_agent_name) }}"></div><div class="col-span-12 md:col-span-6"><label class="ti-form-label">Assign To CRM User *</label><select name="mapped_user_id" class="ti-form-select" required><option value="">Select User</option>@foreach($users as $user)<option value="{{ $user->id }}" {{ old('mapped_user_id',$item->mapped_user_id) === $user->id ? 'selected' : '' }}>{{ $user->name }} @if($user->contact_number) ({{ $user->contact_number }}) @endif</option>@endforeach</select></div><div class="col-span-12"><label class="ti-form-label">Remarks</label><textarea name="remarks" class="form-control">{{ old('remarks',$item->remarks) }}</textarea></div><div class="col-span-12"><label><input type="checkbox" name="is_active" value="1" {{ old('is_active',$item->exists ? $item->is_active : true) ? 'checked' : '' }}> Active</label></div></div><div class="mt-4"><button class="ti-btn ti-btn-primary">Save</button> <a href="{{ route('admin.ivr.agents.index') }}" class="ti-btn ti-btn-light">Cancel</a></div></form></div></div>
@endsection
